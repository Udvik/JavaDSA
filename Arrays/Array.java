import java.util.Scanner;

class Array{
    public static void main(String[] args) {
    /*    singleDimensionalArray sda = new singleDimensionalArray(5);
        sda.insert(0,0);
        sda.insert(1,1);
        sda.insert(2,2);
        sda.insert(3,3);
        
//Accessing  
        sda.valueAt(2);
//traversing
        sda.traversal();
//searching
        sda.searchInArray(2);
//Delete operations
        sda.deleteIndex(0);
        sda.deleteValue(1);
        sda.valueAt(0);
        sda.valueAt(1);*/

//Multidimensional arrays

/*MultiDimensionalArrays mda = new MultiDimensionalArrays(2, 2);
//Inserting
mda.insert(0,0,10);
mda.insert(0,1,20);
mda.insert(0,0,30);
mda.insert(0,0,10);
//Accessing
mda.getValueByIndex(0,0);
//Getting array by using Arrays Library
System.out.println(Arrays.deepToString(mda.arr));

mda.traverse();*/

    }
}